import{NextResponse}from'next/server';export const runtime='nodejs';export async function GET(req){const id=new URL(req.url).searchParams.get('id')||'';if(!/^[0-9a-f]{8}$/i.test(id))return NextResponse.json({ok:false,error:'Invalid question ID.'},{status:400});try{const r=await fetch('https://daily-fifty-api.vercel.app/api/question?id='+encodeURIComponent(id),{next:{revalidate:86400},signal:AbortSignal.timeout(15000)});return new NextResponse(await r.text(),{status:r.status,headers:{'content-type':'application/json','cache-control':'public, s-maxage=86400, stale-while-revalidate=604800'}})}catch(e){return NextResponse.json({ok:false,error:String(e)},{status:502})}}
